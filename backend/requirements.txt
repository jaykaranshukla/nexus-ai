fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.1
python-dotenv==1.0.1

# LangChain
langchain
langchain-community
langchain-google-genai
langchain-text-splitters

# ChromaDB Cloud (client must be >=1.0.6 to match Chroma Cloud's server protocol —
# older clients throw KeyError: '_type' on collection responses)
chromadb==1.1.1

# Other
pypdf==4.2.0